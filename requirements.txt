# Dependencias de todo el proyecto, para el workflow de GitHub Actions.
# Cada paquete tiene su propio requirements.txt para uso local.
aiohttp>=3.9
pandas>=2.1
pyarrow>=15.0
scipy>=1.11
scikit-learn>=1.3
lightgbm>=4.0
matplotlib>=3.8
tqdm>=4.66
